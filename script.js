const cart=JSON.parse(localStorage.getItem('urbanAxisCart')||'[]');function update(){document.querySelectorAll('.count').forEach(x=>x.textContent=cart.length)}document.querySelectorAll('.menu').forEach(b=>b.onclick=()=>document.querySelector('nav').classList.toggle('open'));document.querySelectorAll('.add').forEach(b=>b.onclick=()=>{cart.push({name:b.dataset.name,price:b.dataset.price});localStorage.setItem('urbanAxisCart',JSON.stringify(cart));update()});update();